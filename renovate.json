{
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "description": "Fetch updates for ECR semgrep image from Docker Hub",
      "matchPackageNames": ["/semgrep\\/semgrep$/"],
      "overrideDepName": "semgrep/semgrep",
      "versioning": "docker",
      "overrideDatasource": "docker"
    }
  ]
}